# Vending-Machine
This project implements a vending machine using VHDL, designed as a Finite State Machine (FSM) that accepts coin inputs and dispenses a product when the required amount is reached. The system models the behavior of a real vending machine, handling coin insertion, product delivery, and change return through hardware description and simulation.
